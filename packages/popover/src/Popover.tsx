import {PopoverTrigger} from "./primitives/_popoverTrigger";
import {PopoverRoot} from "./primitives/_popoverRoot";
import {PopoverPortal} from "./primitives/_popoverPortal";
import {PopoverContent} from "./primitives/_popoverContent";

export const Popover = {
    Root: PopoverRoot,
    Trigger: PopoverTrigger,
    Portal: PopoverPortal,
    Content: PopoverContent,
}