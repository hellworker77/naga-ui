import {TooltipRoot} from "./primitives/_tooltipRoot";
import {TooltipTrigger} from "./primitives/_tooltipTrigger";
import {TooltipPortal} from "./primitives/_tooltipPortal";
import {TooltipContent} from "./primitives/_tooltipContent";

export const Tooltip = {
    Root: TooltipRoot,
    Trigger: TooltipTrigger,
    Portal: TooltipPortal,
    Content: TooltipContent,
}